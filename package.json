{
  "name": "electron-app",
  "version": "1.0.2",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/KarthikBharadwajS/electron-update.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-updater": "^4.3.1"
  },
  "devDependencies": {
    "electron": "^9.0.3",
    "electron-builder": "^22.7.0"
  }
}
